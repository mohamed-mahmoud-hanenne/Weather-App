import { BackGroundColorDirective } from './back-ground-color.directive';

describe('BackGroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackGroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
